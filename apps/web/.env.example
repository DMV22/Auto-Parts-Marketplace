# Browser requests use relative /api/* URLs. This server-only value is the
# internal NestJS destination used by the Next.js rewrite.
# Local development uses localhost. Public demo uses the Render HTTPS origin.
# Do not rename this variable with a NEXT_PUBLIC_ prefix.
API_INTERNAL_URL=http://localhost:3001
